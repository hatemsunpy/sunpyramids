services:
  sun-front:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - APP_URL=http://localhost:3000
      - API_URL=https://sunpyramidtours.com/api/
      - APP_ENV=development
    restart: unless-stopped
